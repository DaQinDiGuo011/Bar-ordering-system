<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.yixiang.yshop.module.order.dal.mysql.storeorder.WineStoreMapper">

    <select id="selectPageList" resultType="co.yixiang.yshop.module.order.dal.dataobject.storeorder.WineStoreDO">
        SELECT
        id, user_id, product_id, real_name, store_status, phone, num,spec, remark, store_no, receive_time, create_time, update_time, coupon_id_list,pay_type,actual_pay_price,coupon_price,total_price
        FROM yshop_wine_store
        ${ew.customSqlSegment}
        LIMIT #{offset}, #{size}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM yshop_wine_store
        ${ew.customSqlSegment}
    </select>

</mapper>